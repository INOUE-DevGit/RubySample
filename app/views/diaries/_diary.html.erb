<div id="<%= dom_id diary %>">
  <p>
    <strong>ID:</strong>
    <%= diary.id %>
  </p>

  <p>
    <strong>タイトル:</strong>
    <%= link_to diary.title, diary %>
  </p>

  <p>
    <strong>登録日時:</strong>
    <%= diary.created_at.to_formatted_s %>
  </p>

    <p>
    <strong>更新日時:</strong>
    <%= diary.updated_at.to_formatted_s %>
  </p>
</div>

<%= link_to '日記を新規作成', new_diary_path %>
